"""core.versioning — active version registry stamped on every output."""

from __future__ import annotations

from .registry import VersionRegistry, VersionSet

__all__ = ["VersionRegistry", "VersionSet"]
