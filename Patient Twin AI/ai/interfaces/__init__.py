"""Stable AI interfaces (docs/02 §6).

Deferred DL components are new *implementations* of these protocols, never new
call sites (CLAUDE.md). v1 set: FeatureExtractor, BaselineEngine, Retriever,
Forecaster, LLMGateway. Only the ones needed by the current sprint are present.
"""
