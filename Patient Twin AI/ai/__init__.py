"""AI package: interfaces, prompts, baseline/feature/retrieval implementations.

Placeholder for Sprint 4 (`ai/interfaces/` stable contracts). Present now so
the pytest `testpaths` entry (`pyproject.toml`) resolves cleanly.
"""
